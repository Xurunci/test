spring:
  datasource:
    name: day12  #数据库名
    url: jdbc:mysql://127.0.0.1:3306/day12?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true #url
    username: root  #用户名
    password: admin  #密码
    driver-class-name: com.mysql.cj.jdbc.Driver  #数据库链接驱动

mybatis:
  mapper-locations: classpath:mapper/*.xml  #配置映射文件
  type-aliases-package: com.example.test.bean #配置实体类